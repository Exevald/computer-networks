# Результаты

### Вопрос 1:
> Какие из следующих протоколов показаны как
присутствующие (т.е. перечислены в столбце «protocol»
Wireshark) в вашем файле дампа: TCP, QUIC, HTTP, DNS, UDP,
TLSv1.2?

#### Используемый фильтр:
```
tcp or quic or http or dns or udp or tls
```

#### Ответ:
- TCP
- HTTP
- TLSv1.2

### Вопрос 2:
> Сколько времени прошло с момента отправки HTTP GET
сообщения до получения ответа HTTP OK?

#### Используемый фильтр:
```
http.request.method == "GET" or http.response.code == 200
```

#### Ответ:
```
Arrival Time: Jan 27, 2021 22:22:53.911521000 MSK - запрос с HTTP GET
Arrival Time: Jan 27, 2021 22:22:53.940406000 MSK - ответ с HTTP OK
```
Итоговое время: 0.028885000 сек

### Вопрос 3:
> Каков интернет-адрес gaia.cs.umass.edu (также известного
как www-net.cs.umass.edu)?

#### Используемый фильтр:
```
http contains "gaia.cs.umass.edu" or http contains "www-net.cs.umass.edu"
```

#### Ответ:
128.119.245.12

### Вопрос 4:
> Вы захватили большой объем трафика. Какой фильтр нужно
применить, чтобы отобразить только DNS-запросы и ответы?
Напишите правильный синтаксис.

#### Ответ:
```
dns
```

### Вопрос 5:
> Вам нужно проанализировать весь трафик,
которым обменивается ваш компьютер с адресом 192.168.1.5
и веб-сервером 93.184.216.34 (например, example.com).
Какой фильтр вы используете?

#### Ответ:
```
ip.addr == 192.168.1.5 and ip.addr == 93.184.216.34
```

### Вопрос 6:
> В дампе трафика присутствует служебный протокол SSH (порт
22), который мешает анализу веб-трафика. Какой фильтр
скроет весь SSH-трафик, но покажет все остальное?

#### Ответ:
```
!(tcp.port == 22)
```

### Вопрос 7:
> Вы подозреваете, что на компьютере в вашей сети работает
нелегальный FTP-сервер. FTP использует порт 21 для
управления и порты 20 для данных (active mode). Какой фильтр
поможет найти весь трафик, относящийся к FTP?

#### Ответ:
```
tcp.port == 20 or tcp.port == 21
```

### Вопрос 8:
> Вы обнаружили подозрительный HTTP-запрос на незнакомый сайт. Чтобы проанализировать всю сессию (все запросы и ответы) между вашим компьютером и этим сайтом, нужно отфильтровать трафик по номеру TCP-порта источника и назначения. Какой фильтр будет правильным, если IP вашего компьютера —
192.168.1.10, а IP сервера — 10.10.10.10, и вы установили, что связь шла через порт 54321 вашего ПК и порт 80 сервера?

#### Ответ:
```
(ip.addr == 192.168.1.10 and tcp.port == 54321) and (ip.addr ==
10.10.10.10 and tcp.port == 80)
```